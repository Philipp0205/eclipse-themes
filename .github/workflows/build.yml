name: build

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 25
          cache: maven
      - name: Check token references
        run: ./releng/check-tokens.sh
      - name: Check the GTK palettes against the palette stylesheets
        run: ./releng/check-gtk-palettes.sh
      # GTK skips a rule it cannot parse instead of failing, so a misspelled property
      # would only show as a widget that kept the desktop theme's colour.
      - name: Check the GTK stylesheet parses
        run: |
          sudo apt-get update
          sudo apt-get install --no-install-recommends -y python3-gi gir1.2-gtk-3.0 xvfb
          xvfb-run -a ./releng/check-gtk-stylesheet.py
      # A throwaway key, so that the signing step of the release is exercised on
      # every build without the release key ever leaving the release workflow.
      - name: Prepare a throwaway signing key
        run: |
          export GNUPGHOME=$(mktemp -d)
          gpg --batch --pinentry-mode loopback --passphrase ci-only \
            --quick-generate-key "eclipse-themes CI <ci@vogella.com>" ed25519 sign 1d
          gpg --batch --pinentry-mode loopback --passphrase ci-only \
            --armor --export-secret-keys > "$RUNNER_TEMP/signing-key.asc"
      - name: Build
        env:
          MAVEN_GPG_PASSPHRASE: ci-only
        run: |
          mvn -B clean verify -Dgpg.skip=false \
            -Dtycho.pgp.signer.bc.secretKeys=$RUNNER_TEMP/signing-key.asc
